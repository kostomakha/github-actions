name: Shell commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
        - name: Echo string
          run: echo "github object ${{ github}}".
        - name: multiline script
          run: |
              node -v
              npm -v